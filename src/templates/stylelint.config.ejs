export default {
    extends: [
        "stylelint-config-standard",
        <% if (tools?.includes('Tailwind')) { %>"stylelint-config-tailwindcss",<% } %>
    ],
    plugins: [
        "stylelint-order",
    ],
    rules: {
    }
};
