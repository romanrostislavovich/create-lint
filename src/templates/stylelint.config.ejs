module.exports = {
    extends: [
        "stylelint-config-standard",
        <% if (tools && tools.includes('Prettier')) { %>"stylelint-config-prettier",<% } %>
        <% if (tools && tools.includes('Tailwind')) { %>"stylelint-config-tailwindcss",<% } %>
    ],
    rules: {
        "color-no-invalid-hex": true,
        "block-no-empty": true
    }
};
