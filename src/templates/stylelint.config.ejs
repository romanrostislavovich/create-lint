module.exports = {
    extends: [
        "stylelint-config-standard",
        <% if (tools?.includes('prettier')) { %>"stylelint-config-prettier",<% } %>
        <% if (tools?.includes('tailwind')) { %>"stylelint-config-tailwindcss",<% } %>
    ],
    plugins: [
        <% if (tools?.includes('tailwind')) { %>"stylelint-order",<% } %>
    ],
    rules: {
    }
};
